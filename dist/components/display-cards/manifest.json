{
  "$schema": "http://localhost:3000/schemas/v1.json#",
  "namespace": "ntg-web-design-system",
  "name": "display-cards",
  "displayName": "Display Cards",
  "description": "Responsive grid of card items for NT Government content sections. Metadata-driven manual asset selection.",
  "icon": {
    "id": "view_module",
    "color": {
      "type": "enum",
      "value": "blue"
    }
  },
  "type": "edge",
  "version": "0.3.0",
  "mainFunction": "main",

  "functions": [
    {
      "name": "main",
      "entry": "main.js",

      "input": {
        "type": "object",
        "properties": {
          "selectedCardAssetIds": {
            "type": "array",
            "title": "Select card assets",
            "description": "Matrix assets to display as cards.",
            "items": {
              "type": "string",
              "format": "matrix-asset-uri",
              "title": "Card asset"
            }
          },

          "columns": {
            "type": "number",
            "title": "Columns",
            "enum": [2, 3, 4],
            "default": 3
          },

          "backgroundColor": {
            "type": "string",
            "title": "Background color",
            "enum": ["white", "grey"],
            "default": "white"
          },

          "showDescription": {
            "type": "boolean",
            "title": "Show descriptions",
            "default": true
          },

          "showButton": {
            "type": "boolean",
            "title": "Show buttons",
            "default": true
          },

          "buttonText": {
            "type": "string",
            "title": "Button text",
            "default": "Read more"
          },

          "sectionTitle": {
            "type": "string",
            "title": "Section title"
          },

          "sectionSubtitle": {
            "type": "string",
            "title": "Section subtitle"
          }
        },

        "required": ["selectedCardAssetIds"]
      },

      "output": {
        "responseType": "html"
      }
    }
  ],

  "environment": [
    { "name": "API_IDENTIFIER", "required": false },
    { "name": "BASE_DOMAIN", "required": false },
    { "name": "BASE_PATH", "required": false }
  ],

  "previews": {
    "default": {
      "functionData": {
        "main": {
          "wrapper": { "path": "previews/wrapper.html" },
          "inputData": {
            "type": "file",
            "path": "previews/manual-mode.data.json"
          }
        }
      }
    }
  }
}
